[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "glot-cli"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["polib", "requests"]

[project.scripts]
glot = "glot:main"
